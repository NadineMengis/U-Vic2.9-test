! source file: /net/mare/home1/eby/as/ism/bareland.F
      subroutine bareland(isn)

      use comicegrid
      use comicephys, only: rhoice, rhobed
      use comicesheet, only: hs, h, hb
      use ice_sheet_storage, only: strtis,endis
      use subgrid
      return
      end

